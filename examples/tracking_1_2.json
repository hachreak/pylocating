{
  "version": 1,
  "disable_existing_loggers": false,
  "formatters": {
    "simple": {
      "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    },
    "minimal": {
      "format": "%(asctime)s%(msecs)03d %(message)s",
      "datefmt": "%s"
    }
  },

  "handlers": {
    "LoggingMovingPointFileHandler": {
      "class": "logging.FileHandler",
      "level": "DEBUG",
      "formatter": "minimal",
      "filename": "logs/tracking_1_2-movingpoint.log"
    }
  },

  "loggers": {
    "pylocating.benchmarks.moving_point.LoggingMovingPointEngine": {
      "level": "DEBUG",
      "handlers": ["LoggingMovingPointFileHandler"],
      "propagate": "no"
    }
  }
}

